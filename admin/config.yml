backend:
  name: git-gateway
  branch: main

# Point directly to where your actual images live
media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - label: "Home Page"
        name: "home"
        file: "data/home.json"
        fields:
          - { label: "Main Headline", name: "headline", widget: "string" }
          - { label: "Subheadline", name: "subheadline", widget: "text" }

  - name: "businesses"
    label: "Local Businesses"
    folder: "assets/images/businesses"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Business Name", name: "title", widget: "string" }
      - { label: "Logo / Image", name: "image", widget: "image" }
